'use client';

export function MultiplierBar() {
  return (
    <div className="flex w-full max-w-md justify-between px-1 pb-2">
      {/* Populated with real multipliers from GameConfig in next step */}
      <span className="text-xs text-neutral-500">MultiplierBar</span>
    </div>
  );
}